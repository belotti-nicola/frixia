FEAT:
    HTTPS (integrabile)
    dispatchers
    python integration
    reentrancy check (multiple threads)

FIX:
    logger
    automatic testing

TODO
    refactoring (TCP only TCP, no FrixiaStructures that are used before syscalls)
    parser (UDP/TCP) 
    icon
    create library

CHOICES
    C vs C++