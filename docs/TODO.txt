FEAT:
    parser (UDP/TCP) 
    other listeners services
    dispatchers
    create library
    python integration
    HTTP/UDP/.. to add fd listenings

FIX:
    returning error codes (frixia + errno)
    rename HTTP in TCP
    define start stop in one single point, usable everywhere
    give a proper name to change_epoll_ctl
    HTTPS
    fix cmake build
    logger
    automatic test

