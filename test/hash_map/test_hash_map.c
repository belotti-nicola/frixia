#include "../../src/core/callback_suite/hashmap/hash_map.h"

int main()
{
    HashMap_t* hm = create_hash_map(5);
    
    //implement this.
    //add_entry(hm,NULL);
    
    destroy_hash_map(hm);
    return 0;
}